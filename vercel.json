{
  "buildCommand": "npm install && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "src": "^/api/(.*)",
      "dest": "/api/server.js"
    },
    {
      "src": "^/(.*)",
      "dest": "/index.html"
    }
  ]
}